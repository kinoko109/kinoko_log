export type FrontMatterTypes = {
  title: string;
  description: string;
  tag: string;
  ogpImage: string;
  ogpImageAlt: string;
  created: string;
  updated: string;
};

export type TargetPostData = {
  year: string;
  month: string;
  slug: string;
};
